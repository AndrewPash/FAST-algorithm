cmake_minimum_required(VERSION 3.22.1)
project( angle_detector )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( angle_detector main.cpp )
target_link_libraries( angle_detector ${OpenCV_LIBS} )
SET(GCC_COMPILE_FLAGS "-g -fsanitize=address")
